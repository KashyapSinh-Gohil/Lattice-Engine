"""VAJRA AGRO domain — smallholder climate-risk & yield intelligence.

Mirrors the GRID pack file-for-file (generate/stages/models/scoring/allocate/run) and
reuses the shared core in `pipeline/` (engine.py, io_util.py). Same GPU acceleration story,
different lifeline: villages instead of feeders, irrigation water instead of megawatts.
"""
